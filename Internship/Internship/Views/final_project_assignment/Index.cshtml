@model IEnumerable<Internship.Models.final_project_assignment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.convention.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.validation_group.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.users.email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sheet.categorie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.convention.id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.validation_group.id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.users.email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sheet.categorie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
